class Circle {

    double radius;

    // write methods here
    public double getLength() {
        return Math.PI * 2 * radius;
    }
    public double getArea() {
        return Math.PI * Math.pow(radius, 2);
    }
}